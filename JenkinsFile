pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Starting build..'
                sh  './gradlew build'
            }
        }
        stage('Test') {
            steps {
                echo 'Starting testing..'
                sh './gradlew test'
            }
        }

        }
    }
}